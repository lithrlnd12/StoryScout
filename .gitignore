node_modules/
.local/
.env*
mobile/.env*
web/.env*
local/
.DS_Store
firebase-debug.log
npm-debug.log*
yarn-error.log*

# Service account keys should live in local/ and stay untracked
service-account.json
**/service-account*.json

# Roku development credentials (never commit these)
# Store your Roku IP and password locally, not in repo
roku-config.json
**/roku-config.json
# But allow the example template
!**/roku-config.example.json
